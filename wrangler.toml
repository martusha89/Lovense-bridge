name = "lovense-bridge"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[vars]
LOVENSE_UID = "default"
ALLOWED_ORIGINS = ""

# Secrets (set via wrangler secret put):
# LOVENSE_TOKEN - Your Lovense developer token
# API_SECRET    - Bearer token for REST API authentication
# MCP_SECRET    - Path secret for MCP/SSE endpoints (used in custom connector URL)

[[durable_objects.bindings]]
name = "LOVENSE_CLOUD"
class_name = "LovenseCloud"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["LovenseCloud"]
